#include "main.h"
/**
 * _islower - check if the letter is in lowercase.
 *@c : the char to check
 * Return: 1 if it is lowercase otherwise 0.
 */
int _islower(int c)
{
	if (c >= 'a')
		return (1);
	return (0);
}
